背景音乐目录
==============

将你的中秋古风 BGM 命名为 `bgm.mp3` 放在此目录即可被自动加载：

    public/audio/bgm.mp3

说明：
- 项目启动时会先探测该文件是否存在（HEAD 请求 + Content-Type 校验）。
- 若存在且为音频，则播放本地 mp3。
- 若不存在，则自动降级为 Web Audio API 程序化合成的五声音阶古筝质感环境音，
  无需任何外部音频文件即可开箱听到背景音乐。

因此该目录可以不放置任何文件，站点依然正常工作。
